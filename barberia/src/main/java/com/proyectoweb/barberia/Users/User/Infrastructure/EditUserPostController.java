package com.proyectoweb.barberia.Users.User.Infrastructure;

import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping(value = "/users")
public class EditUserPostController {
    /*
    @PostMapping(value = "/{username}/edit", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<User> execute(@PathVariable("username") String username){
        //TODO: edit user
    }
     */
}
